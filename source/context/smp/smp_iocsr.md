# IOCSR指令与多核通信

按照我们上述的说明，如果使用**按地址访问模式(MMIO)**访问时，寄存器IPI_Status, IPI_Enable,     
IPI_Set, IPI_Clear, MailBox寄存器对于每一个逻辑处理器，都有一个对应的具体地址。

此时操作这些寄存器使用load/store指令，和正常的访存一致。



但是如果采用**配置寄存器指令访问**访问寄存器时，只需要知道IOCSR指令地址空间就可以。当每个逻辑处理器    
执行指令IOCSR时，都会执行处理各自相关的IPI寄存器。

